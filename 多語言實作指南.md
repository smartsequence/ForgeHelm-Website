# 智序資訊工作室 - 多語言實作指南（已部分過時）

> **狀態說明**：本文件為 2026-01-25 的早期多語言技術筆記，  
> 目前實際實作與檔案結構請以 `I18N_SETUP.md` 與 `AI自動翻譯方案.md` 為主，本文件僅保留做背景參考。

**建立日期**: 2026-01-25  
**支援語言**: 7 種  
**狀態**: 📋 詳細實作方案

---

## 🌍 支援的語言清單

| 代碼 | 語言 | 英文名稱 | 優先級 | RTL |
|------|------|---------|--------|-----|
| zh-TW | 繁體中文 | Traditional Chinese | P0 | ❌ |
| en | English | English | P0 | ❌ |
| ja | 日本語 | Japanese | P1 | ❌ |
| de | Deutsch | German | P1 | ❌ |
| es | Español | Spanish | P1 | ❌ |
| fr | Français | French | P1 | ❌ |
| he | עברית | Hebrew | P2 | ✅ |

**優先級說明：**
- **P0**: 必須完成（繁中、英文）
- **P1**: 重要市場（日、德、西、法）
- **P2**: 特殊需求（希伯來文 - RTL 支援）

---

## 📁 完整專案結構

```
DocEngine-Website/
│
├─ src/
│  ├─ i18n/
│  │  ├─ ui.ts                    # 翻譯工具函式
│  │  ├─ utils.ts                 # 輔助函式
│  │  └─ locales/
│  │     ├─ zh-TW.json           # 繁體中文 ⭐
│  │     ├─ en.json              # English ⭐
│  │     ├─ ja.json              # 日本語
│  │     ├─ de.json              # Deutsch
│  │     ├─ es.json              # Español
│  │     ├─ fr.json              # Français
│  │     └─ he.json              # עברית (RTL)
│  │
│  ├─ pages/
│  │  ├─ index.astro             # 繁中首頁
│  │  ├─ doc-engine.astro        # 繁中產品頁
│  │  ├─ pricing.astro           # 繁中定價
│  │  ├─ about.astro             # 繁中關於
│  │  ├─ contact.astro           # 繁中聯絡
│  │  │
│  │  ├─ en/
│  │  │  ├─ index.astro
│  │  │  ├─ doc-engine.astro
│  │  │  ├─ pricing.astro
│  │  │  ├─ about.astro
│  │  │  └─ contact.astro
│  │  │
│  │  ├─ ja/                     # 日文頁面
│  │  ├─ de/                     # 德文頁面
│  │  ├─ es/                     # 西班牙文頁面
│  │  ├─ fr/                     # 法文頁面
│  │  └─ he/                     # 希伯來文頁面（RTL）
│  │
│  └─ components/
│     ├─ LanguageSwitcher.astro  # 語言切換器
│     └─ LanguageDropdown.astro  # 語言下拉選單（多語言）
│
├─ scripts/
│  ├─ translate.js               # 翻譯腳本
│  └─ translate-all.js           # 批次翻譯所有語言
│
└─ public/
   └─ flags/                     # 國旗圖示（可選）
      ├─ tw.svg
      ├─ us.svg
      ├─ jp.svg
      ├─ de.svg
      ├─ es.svg
      ├─ fr.svg
      └─ il.svg
```

---

## ⚙️ Astro 配置

### `astro.config.mjs`

```javascript
import { defineConfig } from 'astro/config';
import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://smartsequence.tech',
  
  // 多語言配置
  i18n: {
    defaultLocale: 'zh-TW',
    locales: ['zh-TW', 'en', 'ja', 'de', 'es', 'fr', 'he'],
    routing: {
      prefixDefaultLocale: false, // zh-TW 不加前綴
    },
    // 語言備援設定
    fallback: {
      ja: 'en',  // 日文缺失時顯示英文
      de: 'en',
      es: 'en',
      fr: 'en',
      he: 'en',
    },
  },
  
  integrations: [
    mdx(), 
    sitemap({
      i18n: {
        defaultLocale: 'zh-TW',
        locales: {
          'zh-TW': 'zh-TW',
          en: 'en',
          ja: 'ja',
          de: 'de',
          es: 'es',
          fr: 'fr',
          he: 'he',
        },
      },
    }),
  ],
  
  vite: {
    plugins: [tailwindcss()],
  },
});
```

---

## 📝 翻譯字典範例

### `src/i18n/locales/zh-TW.json`

```json
{
  "nav": {
    "home": "首頁",
    "docengine": "DocEngine",
    "pricing": "定價",
    "about": "關於",
    "contact": "聯絡"
  },
  "hero": {
    "title": "讓 AI 為您的程式碼說故事",
    "subtitle": "自動分析 .NET 系統，生成專業技術文件，節省 80% 時間",
    "cta": "免費試用 14 天",
    "learnMore": "了解更多"
  },
  "languages": {
    "zh-TW": "繁體中文",
    "en": "English",
    "ja": "日本語",
    "de": "Deutsch",
    "es": "Español",
    "fr": "Français",
    "he": "עברית"
  },
  "footer": {
    "taxId": "統一編號",
    "tagline": "讓 AI 為您的程式碼說故事",
    "copyright": "© 2026 智序資訊工作室. 版權所有."
  }
}
```

### `src/i18n/locales/en.json`

```json
{
  "nav": {
    "home": "Home",
    "docengine": "DocEngine",
    "pricing": "Pricing",
    "about": "About",
    "contact": "Contact"
  },
  "hero": {
    "title": "Let AI Tell Your Code's Story",
    "subtitle": "Automatically analyze .NET systems and generate professional technical documentation, saving 80% of time",
    "cta": "Free 14-Day Trial",
    "learnMore": "Learn More"
  },
  "languages": {
    "zh-TW": "繁體中文",
    "en": "English",
    "ja": "日本語",
    "de": "Deutsch",
    "es": "Español",
    "fr": "Français",
    "he": "עברית"
  },
  "footer": {
    "taxId": "Tax ID",
    "tagline": "Let AI Tell Your Code's Story",
    "copyright": "© 2026 SmartSequence Tech Studio. All rights reserved."
  }
}
```

---

## 🤖 批次翻譯腳本

### `scripts/translate-all.js`

```javascript
import OpenAI from 'openai';
import fs from 'fs/promises';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

// 語言配置
const LANGUAGES = {
  'en': {
    name: 'English',
    prompt: 'professional and clear English suitable for business websites',
  },
  'ja': {
    name: 'Japanese',
    prompt: 'polite and professional Japanese (です・ます調)',
  },
  'de': {
    name: 'German',
    prompt: 'professional German suitable for business',
  },
  'es': {
    name: 'Spanish',
    prompt: 'professional Spanish (formal)',
  },
  'fr': {
    name: 'French',
    prompt: 'professional French suitable for business',
  },
  'he': {
    name: 'Hebrew',
    prompt: 'professional Hebrew suitable for business websites',
  },
};

/**
 * 翻譯到指定語言
 */
async function translateToLanguage(zhText, targetLang, context = '') {
  const langConfig = LANGUAGES[targetLang];
  
  console.log(`  🔄 Translating to ${langConfig.name}...`);
  
  const response = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      {
        role: 'system',
        content: `You are a professional translator specializing in technical and business content.
Translate Traditional Chinese (Taiwan) to ${langConfig.name}.
Use ${langConfig.prompt}.
Maintain technical terms accuracy.
${context ? `Context: ${context}` : ''}`,
      },
      {
        role: 'user',
        content: `Translate this Traditional Chinese text to ${langConfig.name}:\n\n${zhText}`,
      },
    ],
    temperature: 0.3,
  });

  return response.choices[0].message.content;
}

/**
 * 驗證繁中翻譯（防止大陸用詞）
 */
async function validateTraditionalChinese(enText, originalZhText) {
  console.log(`  🔍 Validating Traditional Chinese (Taiwan terminology)...`);
  
  const response = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      {
        role: 'system',
        content: `You are a translator specializing in Traditional Chinese (Taiwan).
Translate English back to Traditional Chinese using TAIWAN terminology.

CRITICAL - Use Taiwan terms ONLY:
✅ 智慧 (NOT 智能)
✅ 軟體 (NOT 软件/軟件)
✅ 網路 (NOT 网络/網絡)
✅ 資料 (NOT 数据/數據)
✅ 程式 (NOT 程序)
✅ 伺服器 (NOT 服务器/伺服器)
✅ 使用者 (NOT 用户/用戶)
✅ 管道 (NOT 渠道)
✅ 資訊 (NOT 信息)
✅ 線上 (NOT 在线/在線)

Maintain professional business tone.`,
      },
      {
        role: 'user',
        content: `Translate this English text back to Traditional Chinese (Taiwan):\n\n${enText}\n\nOriginal reference:\n${originalZhText}`,
      },
    ],
    temperature: 0.3,
  });

  return response.choices[0].message.content;
}

/**
 * 翻譯 JSON 物件
 */
async function translateObject(obj, targetLang, path = '') {
  const result = {};

  for (const [key, value] of Object.entries(obj)) {
    const currentPath = path ? `${path}.${key}` : key;
    
    if (typeof value === 'string') {
      console.log(`\n  📝 ${currentPath}`);
      console.log(`     原文: ${value}`);
      
      const translation = await translateToLanguage(value, targetLang, currentPath);
      console.log(`     譯文: ${translation}`);
      
      result[key] = translation;
      
      // 延遲避免 rate limit
      await new Promise(resolve => setTimeout(resolve, 1000));
    } else if (typeof value === 'object' && value !== null) {
      result[key] = await translateObject(value, targetLang, currentPath);
    } else {
      result[key] = value;
    }
  }

  return result;
}

/**
 * 主要翻譯流程
 */
async function translateAll() {
  console.log('🌍 DocEngine Multi-language Translation Tool\n');
  
  // 讀取繁中原文
  const sourceFile = './src/i18n/locales/zh-TW.json';
  console.log(`📖 Reading source file: ${sourceFile}`);
  const sourceContent = await fs.readFile(sourceFile, 'utf-8');
  const sourceJson = JSON.parse(sourceContent);
  
  // 翻譯到各語言
  for (const [langCode, langConfig] of Object.entries(LANGUAGES)) {
    console.log(`\n${'='.repeat(60)}`);
    console.log(`🌐 Translating to ${langConfig.name} (${langCode})`);
    console.log('='.repeat(60));
    
    try {
      const translated = await translateObject(sourceJson, langCode);
      
      const targetFile = `./src/i18n/locales/${langCode}.json`;
      await fs.writeFile(
        targetFile,
        JSON.stringify(translated, null, 2),
        'utf-8'
      );
      
      console.log(`\n✅ ${langConfig.name} translation saved to ${targetFile}`);
    } catch (error) {
      console.error(`\n❌ Error translating to ${langConfig.name}:`, error.message);
    }
  }
  
  // 驗證繁中用詞
  console.log(`\n${'='.repeat(60)}`);
  console.log(`🔍 Validating Traditional Chinese (Taiwan) terminology`);
  console.log('='.repeat(60));
  
  try {
    const enFile = './src/i18n/locales/en.json';
    const enContent = await fs.readFile(enFile, 'utf-8');
    const enJson = JSON.parse(enContent);
    
    // 抽樣驗證幾個關鍵詞
    const keysToValidate = [
      'hero.title',
      'hero.subtitle',
    ];
    
    for (const keyPath of keysToValidate) {
      const keys = keyPath.split('.');
      let enValue = enJson;
      let zhValue = sourceJson;
      
      for (const k of keys) {
        enValue = enValue[k];
        zhValue = zhValue[k];
      }
      
      console.log(`\n  檢查: ${keyPath}`);
      console.log(`  原文: ${zhValue}`);
      console.log(`  英文: ${enValue}`);
      
      const validated = await validateTraditionalChinese(enValue, zhValue);
      console.log(`  驗證: ${validated}`);
      
      if (validated !== zhValue) {
        console.log(`  ⚠️  用詞可能需要調整！`);
      } else {
        console.log(`  ✅ 用詞正確`);
      }
      
      await new Promise(resolve => setTimeout(resolve, 1000));
    }
  } catch (error) {
    console.error(`\n❌ Error validating Chinese:`, error.message);
  }
  
  console.log(`\n${'='.repeat(60)}`);
  console.log('🎉 All translations completed!');
  console.log('='.repeat(60));
  console.log('\n⚠️  Important: Please manually review all translations!');
  console.log('📝 Check technical terms and professional tone.');
}

// 執行
translateAll().catch(err => {
  console.error('\n💥 Fatal error:', err);
  process.exit(1);
});
```

### 使用方式

```bash
# 安裝依賴
npm install openai

# 設定環境變數（在 .env 檔案中）
OPENAI_API_KEY=sk-your-api-key-here

# 執行批次翻譯
node scripts/translate-all.js
```

---

## 🎨 多語言下拉選單元件

### `src/components/LanguageDropdown.astro`

```astro
---
import { languages } from '../i18n/utils';

interface Props {
  currentLang: string;
  currentPath: string;
}

const { currentLang, currentPath } = Astro.props;

// 取得當前語言的完整名稱
const currentLangName = languages[currentLang as keyof typeof languages];

// 計算其他語言的 URL
function getLocalizedUrl(locale: string, path: string) {
  // 移除現有的語言前綴
  let cleanPath = path.replace(/^\/(en|ja|de|es|fr|he)/, '');
  if (!cleanPath) cleanPath = '/';
  
  // 繁中不加前綴，其他語言加前綴
  if (locale === 'zh-TW') {
    return cleanPath;
  }
  return `/${locale}${cleanPath}`;
}
---

<div class="language-dropdown">
  <button class="dropdown-toggle" aria-label="選擇語言">
    <span class="current-lang">🌐 {currentLangName}</span>
    <svg class="dropdown-icon" width="12" height="12" viewBox="0 0 12 12" fill="none">
      <path d="M2 4L6 8L10 4" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
    </svg>
  </button>
  
  <div class="dropdown-menu">
    {Object.entries(languages).map(([code, name]) => (
      <a
        href={getLocalizedUrl(code, currentPath)}
        class:list={[
          'dropdown-item',
          { active: currentLang === code }
        ]}
        hreflang={code}
      >
        <span class="lang-flag">{getFlagEmoji(code)}</span>
        <span class="lang-name">{name}</span>
        {currentLang === code && <span class="check">✓</span>}
      </a>
    ))}
  </div>
</div>

<script>
  // 下拉選單互動
  document.addEventListener('DOMContentLoaded', () => {
    const dropdown = document.querySelector('.language-dropdown');
    const toggle = dropdown?.querySelector('.dropdown-toggle');
    const menu = dropdown?.querySelector('.dropdown-menu');

    toggle?.addEventListener('click', () => {
      dropdown?.classList.toggle('open');
    });

    // 點擊外部關閉
    document.addEventListener('click', (e) => {
      if (!dropdown?.contains(e.target as Node)) {
        dropdown?.classList.remove('open');
      }
    });
  });
</script>

<style>
  .language-dropdown {
    position: relative;
    display: inline-block;
  }

  .dropdown-toggle {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border: 1px solid var(--gray-300);
    border-radius: 0.5rem;
    background: white;
    cursor: pointer;
    transition: all 0.2s;
  }

  .dropdown-toggle:hover {
    border-color: var(--primary);
    background: var(--gray-50);
  }

  .dropdown-icon {
    transition: transform 0.2s;
  }

  .language-dropdown.open .dropdown-icon {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: calc(100% + 0.5rem);
    right: 0;
    min-width: 200px;
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: 0.5rem;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.2s;
    z-index: 100;
    max-height: 400px;
    overflow-y: auto;
  }

  .language-dropdown.open .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    color: var(--gray-700);
    text-decoration: none;
    transition: all 0.2s;
    border-bottom: 1px solid var(--gray-100);
  }

  .dropdown-item:last-child {
    border-bottom: none;
  }

  .dropdown-item:hover {
    background: var(--gray-50);
    color: var(--primary);
  }

  .dropdown-item.active {
    background: var(--primary-50);
    color: var(--primary);
    font-weight: 600;
  }

  .lang-flag {
    font-size: 1.25rem;
  }

  .lang-name {
    flex: 1;
  }

  .check {
    color: var(--primary);
    font-weight: bold;
  }
</style>

<script is:inline define:vars={{ languages }}>
  function getFlagEmoji(code) {
    const flags = {
      'zh-TW': '🇹🇼',
      'en': '🇺🇸',
      'ja': '🇯🇵',
      'de': '🇩🇪',
      'es': '🇪🇸',
      'fr': '🇫🇷',
      'he': '🇮🇱',
    };
    return flags[code] || '🌐';
  }
</script>
```

---

## 🔄 RTL（從右到左）支援

### 希伯來文特殊處理

#### `src/layouts/BaseLayout.astro`

```astro
---
const { lang = 'zh-TW' } = Astro.props;
const isRTL = lang === 'he'; // 希伯來文是 RTL
---

<!DOCTYPE html>
<html lang={lang} dir={isRTL ? 'rtl' : 'ltr'}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- ... other meta tags ... -->
  </head>
  <body class:list={{ rtl: isRTL }}>
    <slot />
  </body>
</html>

<style is:global>
  /* RTL 特殊樣式 */
  .rtl {
    direction: rtl;
    text-align: right;
  }

  .rtl .flex {
    flex-direction: row-reverse;
  }

  /* Tailwind RTL 覆寫 */
  .rtl .ml-4 {
    margin-left: 0;
    margin-right: 1rem;
  }

  .rtl .mr-4 {
    margin-right: 0;
    margin-left: 1rem;
  }

  .rtl .text-left {
    text-align: right;
  }

  .rtl .text-right {
    text-align: left;
  }
</style>
```

---

## 🚀 分階段推出策略

### Phase 1: 核心語言（1 週）

**P0 - 立即完成：**
- ✅ 繁體中文（預設）
- ✅ English

**目標：**
- 覆蓋 80% 的目標市場
- SEO 優化完成
- 測試多語言架構

### Phase 2: 主要市場（2 週）

**P1 - 優先市場：**
- 🇯🇵 日本語（亞洲市場）
- 🇩🇪 Deutsch（歐洲市場）
- 🇪🇸 Español（拉美市場）
- 🇫🇷 Français（法語區市場）

**執行方式：**
1. 使用 OpenAI 批次翻譯
2. 人工校對技術術語
3. 測試每個語言版本
4. 逐步上線

### Phase 3: 特殊需求（1 週）

**P2 - 特殊市場：**
- 🇮🇱 עברית（以色列市場 + RTL 支援）

**額外工作：**
- RTL 佈局調整
- 特殊字型處理
- 詳細測試

---

## 💰 完整成本分析

### 開發成本（一次性）

```
OpenAI API 翻譯成本：

繁中原文：
- 5 個主要頁面
- 每頁 2000 tokens
- 總計：10,000 tokens

翻譯到 6 種語言：
- 輸入：10K × 6 = 60K tokens
- 輸出：10K × 6 = 60K tokens
- 成本：
  - Input: 60K × $0.01 / 1000 = $0.60
  - Output: 60K × $0.03 / 1000 = $1.80
  
繁中驗證（英文→繁中）：
- 輸入：10K
- 輸出：10K
- 成本：$0.40

總成本：$0.60 + $1.80 + $0.40 = $2.80 ✅

人工校對時間：
- 每個語言 2-3 小時
- 6 個語言 = 12-18 小時
- 可分批進行
```

### 運營成本（每月）

```
Vercel 部署：
- 所有語言都是靜態生成
- 無執行時成本
- Free tier 足夠使用

成本：$0/月 ✅
```

---

## 📊 SEO 優化

### hreflang 標籤設定

```astro
---
const allLanguages = ['zh-TW', 'en', 'ja', 'de', 'es', 'fr', 'he'];
---

<head>
  <!-- Canonical -->
  <link rel="canonical" href={canonicalURL} />
  
  <!-- hreflang for all languages -->
  {allLanguages.map(locale => (
    <link 
      rel="alternate" 
      hreflang={locale} 
      href={getLocalizedUrl(locale, currentPath)} 
    />
  ))}
  
  <!-- Default fallback -->
  <link rel="alternate" hreflang="x-default" href={defaultURL} />
</head>
```

---

## ✅ 檢查清單

### 翻譯完成檢查

- [ ] 所有 JSON 翻譯檔案已建立
- [ ] OpenAI 翻譯完成
- [ ] 人工校對完成
- [ ] 技術術語正確
- [ ] 語氣專業一致

### 頁面建立檢查

- [ ] 各語言目錄結構建立
- [ ] 所有頁面已複製並翻譯
- [ ] 語言切換器正常運作
- [ ] URL 結構正確

### SEO 檢查

- [ ] 每頁都有正確的 lang 屬性
- [ ] hreflang 標籤設定完成
- [ ] Sitemap 包含所有語言
- [ ] Meta 標籤翻譯完成

### RTL 檢查（希伯來文）

- [ ] dir="rtl" 屬性設定
- [ ] 佈局方向正確
- [ ] 文字對齊正確
- [ ] 圖示方向調整

### 測試檢查

- [ ] 所有語言版本可正常訪問
- [ ] 語言切換功能正常
- [ ] 響應式設計正常
- [ ] 效能測試通過（Lighthouse）

---

## 🎯 推薦實作順序

### 建議：漸進式推出 ⭐

```
Week 1-2: 繁中版本完成
  └─ 專注內容開發
  └─ 確保品質

Week 3: 加入英文版
  ├─ 設定 Astro i18n
  ├─ OpenAI 翻譯
  ├─ 人工校對
  └─ 測試上線

Week 4-5: 加入其他語言
  ├─ 日文、德文、西班牙文、法文
  ├─ 批次翻譯
  ├─ 分批校對
  └─ 逐步上線

Week 6: 希伯來文 + RTL
  ├─ RTL 佈局調整
  ├─ 翻譯與測試
  └─ 完成所有語言
```

---

**建立日期**: 2026-01-25  
**維護者**: 智序資訊工作室  
**狀態**: ✅ 完整實作指南

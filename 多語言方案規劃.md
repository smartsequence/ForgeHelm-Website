# 智序資訊工作室官網 - 多語言方案規劃

**建立日期**: 2026-01-25  
**版本**: v1.0  
**狀態**: 📋 規劃中

---

## 🌍 方案比較與推薦

### 方案 1: Astro i18n Routing ⭐ **強烈推薦**

**架構：**
```
smartsequence.tech
├─ /                    → 繁體中文 (預設)
├─ /en/                 → English
└─ /ja/                 → 日本語 (未來)
```

**優點：**
- ✅ Astro 官方支援，穩定可靠
- ✅ SEO 友善（每個語言獨立 URL）
- ✅ 靜態生成，速度極快
- ✅ 無執行時成本
- ✅ 完全離線可用

**缺點：**
- ⚠️ 內容需要預先翻譯好
- ⚠️ 新增語言需要重新建置

**成本：**
- 部署：$0（仍在 Vercel 免費額度內）
- 執行：$0（靜態頁面）
- 翻譯：一次性成本

---

### 方案 2: OpenAI API 即時翻譯 ❌ **不推薦**

**架構：**
- 使用者訪問時即時呼叫 OpenAI API 翻譯

**缺點：**
- ❌ 成本高（每次訪問都要 API 呼叫）
- ❌ 速度慢（需要等待 API 回應）
- ❌ 不穩定（API 可能失敗）
- ❌ SEO 不友善（搜尋引擎看不到翻譯內容）
- ❌ 無法離線

**預估成本：**
```
假設：
- 每頁 2000 tokens
- 每月 1000 訪客
- GPT-4 Turbo: $0.01/1K tokens

成本 = 2 × 1000 × $0.01 = $20/月

而且這還不包括：
- API 失敗重試
- 快取機制開發成本
- 維護成本
```

---

### 方案 3: 混合方案 ⭐ **我的推薦**

**核心概念：**
- 使用 Astro i18n 靜態生成
- 開發時用 OpenAI API 輔助翻譯
- 人工校對確保品質

**流程：**
```
1. 撰寫繁體中文內容
   ↓
2. 使用 OpenAI API 建置時翻譯
   繁中 → 英文 → 繁中檢查（避免大陸用詞）
   ↓
3. 人工校對英文翻譯
   ↓
4. 建置靜態多語言網站
   ↓
5. 部署到 Vercel（$0）
```

**優點：**
- ✅ 結合 AI 效率 + 人工品質
- ✅ 生產環境零成本
- ✅ SEO 完美支援
- ✅ 速度極快
- ✅ 可控制品質

---

## 🎯 推薦方案詳細實作

### 階段 1: 設定 Astro i18n

#### 1.1 更新 `astro.config.mjs`

```javascript
import { defineConfig } from 'astro/config';
import mdx from '@astrojs/mdx';
import sitemap from '@astrojs/sitemap';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://smartsequence.tech',
  
  // 多語言配置
  i18n: {
    defaultLocale: 'zh-TW',
    locales: ['zh-TW', 'en'],
    routing: {
      prefixDefaultLocale: false, // zh-TW 不加前綴
    },
  },
  
  integrations: [mdx(), sitemap()],
  
  vite: {
    plugins: [tailwindcss()],
  },
});
```

#### 1.2 建立翻譯檔案結構

```
src/
├─ i18n/
│  ├─ ui.ts                    # UI 翻譯字典
│  ├─ utils.ts                 # 翻譯工具函式
│  └─ locales/
│     ├─ zh-TW.json           # 繁體中文
│     └─ en.json              # English
│
├─ content/
│  ├─ pages/
│  │  ├─ zh-TW/              # 繁中內容
│  │  │  ├─ home.md
│  │  │  ├─ about.md
│  │  │  └─ ...
│  │  └─ en/                 # 英文內容
│  │     ├─ home.md
│  │     ├─ about.md
│  │     └─ ...
```

#### 1.3 建立翻譯字典 `src/i18n/locales/zh-TW.json`

```json
{
  "nav": {
    "home": "首頁",
    "docengine": "DocEngine",
    "pricing": "定價",
    "about": "關於",
    "contact": "聯絡"
  },
  "common": {
    "learnMore": "了解更多",
    "getStarted": "開始使用",
    "freeTrial": "免費試用",
    "contactUs": "聯絡我們"
  },
  "footer": {
    "taxId": "統一編號",
    "copyright": "© 2026 智序資訊工作室. 版權所有."
  }
}
```

#### 1.4 建立翻譯字典 `src/i18n/locales/en.json`

```json
{
  "nav": {
    "home": "Home",
    "docengine": "DocEngine",
    "pricing": "Pricing",
    "about": "About",
    "contact": "Contact"
  },
  "common": {
    "learnMore": "Learn More",
    "getStarted": "Get Started",
    "freeTrial": "Free Trial",
    "contactUs": "Contact Us"
  },
  "footer": {
    "taxId": "Tax ID",
    "copyright": "© 2026 SmartSequence Tech Studio. All rights reserved."
  }
}
```

#### 1.5 建立翻譯工具 `src/i18n/utils.ts`

```typescript
import zhTW from './locales/zh-TW.json';
import en from './locales/en.json';

export const languages = {
  'zh-TW': '繁體中文',
  'en': 'English',
};

export const defaultLang = 'zh-TW';

export const ui = {
  'zh-TW': zhTW,
  'en': en,
} as const;

export function getLangFromUrl(url: URL) {
  const [, lang] = url.pathname.split('/');
  if (lang in ui) return lang as keyof typeof ui;
  return defaultLang;
}

export function useTranslations(lang: keyof typeof ui) {
  return function t(key: string) {
    const keys = key.split('.');
    let value: any = ui[lang];
    
    for (const k of keys) {
      value = value?.[k];
    }
    
    return value || key;
  };
}
```

#### 1.6 更新頁面使用翻譯

**範例：`src/pages/index.astro`（繁中）**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { useTranslations } from '../i18n/utils';

const lang = 'zh-TW';
const t = useTranslations(lang);
---

<BaseLayout title="智序資訊工作室" lang={lang}>
  <h1>{t('nav.home')}</h1>
  <button>{t('common.freeTrial')}</button>
</BaseLayout>
```

**範例：`src/pages/en/index.astro`（英文）**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { useTranslations } from '../../i18n/utils';

const lang = 'en';
const t = useTranslations(lang);
---

<BaseLayout title="SmartSequence Tech Studio" lang={lang}>
  <h1>{t('nav.home')}</h1>
  <button>{t('common.freeTrial')}</button>
</BaseLayout>
```

---

### 階段 2: OpenAI 輔助翻譯工具

#### 2.1 建立翻譯腳本 `scripts/translate.js`

```javascript
import OpenAI from 'openai';
import fs from 'fs/promises';
import path from 'path';

// 從環境變數讀取 API Key
const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

/**
 * 智序的翻譯策略：先譯英文，再譯繁中
 * 避免 AI 直接生成繁中時使用大陸用詞
 */
async function translateToEnglish(zhText) {
  const response = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      {
        role: 'system',
        content: `You are a professional translator specializing in technical and business content. 
Translate Traditional Chinese (Taiwan) to English.
Maintain technical terms accuracy and professional tone.`,
      },
      {
        role: 'user',
        content: `Please translate the following Traditional Chinese text to English:\n\n${zhText}`,
      },
    ],
    temperature: 0.3, // 降低創意，提高準確性
  });

  return response.choices[0].message.content;
}

/**
 * 驗證繁中翻譯（避免大陸用詞）
 */
async function validateTraditionalChinese(enText, originalZhText) {
  const response = await openai.chat.completions.create({
    model: 'gpt-4-turbo-preview',
    messages: [
      {
        role: 'system',
        content: `You are a translator specializing in Traditional Chinese (Taiwan).
Translate English to Traditional Chinese using TAIWAN terminology.

IMPORTANT - Use Taiwan terms, NOT Mainland China terms:
- 智慧 (not 智能)
- 軟體 (not 软件)
- 網路 (not 网络)
- 資料 (not 数据)
- 程式 (not 程序)
- 伺服器 (not 服务器)
- 使用者 (not 用户)
- 管道 (not 渠道)

Maintain professional tone suitable for business website.`,
      },
      {
        role: 'user',
        content: `Translate to Traditional Chinese (Taiwan):\n\n${enText}\n\nOriginal text for reference:\n${originalZhText}`,
      },
    ],
    temperature: 0.3,
  });

  return response.choices[0].message.content;
}

/**
 * 翻譯單個 JSON 物件
 */
async function translateObject(obj, sourceLang = 'zh-TW') {
  const result = {};

  for (const [key, value] of Object.entries(obj)) {
    if (typeof value === 'string') {
      console.log(`  Translating: ${key}...`);
      
      // 先翻成英文
      const enTranslation = await translateToEnglish(value);
      console.log(`    EN: ${enTranslation}`);
      
      // 再翻回繁中驗證
      const zhValidation = await validateTraditionalChinese(enTranslation, value);
      console.log(`    ZH Validation: ${zhValidation}`);
      
      result[key] = enTranslation;
      
      // 延遲避免 API rate limit
      await new Promise(resolve => setTimeout(resolve, 1000));
    } else if (typeof value === 'object') {
      result[key] = await translateObject(value, sourceLang);
    }
  }

  return result;
}

/**
 * 主要翻譯函式
 */
async function translateLocaleFile(sourceFile, targetFile) {
  console.log(`\n📖 Reading ${sourceFile}...`);
  const sourceContent = await fs.readFile(sourceFile, 'utf-8');
  const sourceJson = JSON.parse(sourceContent);

  console.log(`\n🔄 Translating...`);
  const translatedJson = await translateObject(sourceJson);

  console.log(`\n💾 Writing to ${targetFile}...`);
  await fs.writeFile(
    targetFile,
    JSON.stringify(translatedJson, null, 2),
    'utf-8'
  );

  console.log(`\n✅ Translation complete!`);
}

// 執行翻譯
const sourceFile = './src/i18n/locales/zh-TW.json';
const targetFile = './src/i18n/locales/en.json';

translateLocaleFile(sourceFile, targetFile)
  .then(() => console.log('\n🎉 All done!'))
  .catch(err => {
    console.error('\n❌ Error:', err);
    process.exit(1);
  });
```

#### 2.2 更新 `package.json` 加入翻譯指令

```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "translate": "node scripts/translate.js"
  }
}
```

#### 2.3 設定環境變數

建立 `.env` 檔案（不要提交到 Git）：

```bash
OPENAI_API_KEY=sk-your-api-key-here
```

更新 `.gitignore`：

```
.env
.env.local
```

---

### 階段 3: 語言切換 UI

#### 3.1 建立語言切換元件 `src/components/LanguageSwitcher.astro`

```astro
---
import { languages } from '../i18n/utils';

interface Props {
  currentLang: string;
  currentPath: string;
}

const { currentLang, currentPath } = Astro.props;

// 計算其他語言的 URL
function getLocalizedUrl(locale: string, path: string) {
  if (locale === 'zh-TW') {
    return path.replace(/^\/en/, '') || '/';
  }
  return `/en${path}`;
}
---

<div class="language-switcher">
  {Object.entries(languages).map(([code, name]) => (
    <a
      href={getLocalizedUrl(code, currentPath)}
      class:list={[
        'lang-link',
        { active: currentLang === code }
      ]}
      aria-label={`Switch to ${name}`}
    >
      {name}
    </a>
  ))}
</div>

<style>
  .language-switcher {
    display: flex;
    gap: 1rem;
  }

  .lang-link {
    padding: 0.5rem 1rem;
    border-radius: 0.375rem;
    text-decoration: none;
    transition: all 0.2s;
  }

  .lang-link:hover {
    background-color: var(--gray-100);
  }

  .lang-link.active {
    background-color: var(--primary);
    color: white;
    font-weight: 600;
  }
</style>
```

#### 3.2 在 Header 中加入語言切換器

```astro
---
// src/components/Header.astro
import LanguageSwitcher from './LanguageSwitcher.astro';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<header>
  <nav>
    <a href="/">{t('nav.home')}</a>
    <a href="/doc-engine">{t('nav.docengine')}</a>
    <a href="/pricing">{t('nav.pricing')}</a>
    <a href="/about">{t('nav.about')}</a>
    <a href="/contact">{t('nav.contact')}</a>
    
    <LanguageSwitcher 
      currentLang={lang} 
      currentPath={Astro.url.pathname}
    />
  </nav>
</header>
```

---

## 💰 成本分析

### 開發階段（一次性）

```
假設需要翻譯：
- 5 個主要頁面
- 每頁約 2000 tokens 內容
- 使用 GPT-4 Turbo

計算：
- 輸入：5 × 2000 = 10,000 tokens
- 輸出：約 10,000 tokens
- 驗證回譯：再 × 2 = 40,000 tokens 總計

成本：
- GPT-4 Turbo Input: $0.01/1K = $0.10
- GPT-4 Turbo Output: $0.03/1K = $0.30
總計：約 $0.40 ✅ 非常便宜！
```

### 生產環境（持續）

```
成本：$0 ✅
- 所有頁面都是靜態生成
- 不需要執行時翻譯
- 部署在 Vercel Free tier
```

---

## 📊 工作流程

### 新增/更新內容時

```
1. 編輯繁中內容
   └─ src/i18n/locales/zh-TW.json
   └─ src/content/pages/zh-TW/*.md

2. 執行翻譯腳本
   $ npm run translate
   
3. 檢查生成的英文翻譯
   └─ src/i18n/locales/en.json
   
4. 人工校對（重要！）
   - 檢查技術術語
   - 檢查語氣是否專業
   - 修正任何不自然的表達

5. 更新英文內容頁面
   └─ src/content/pages/en/*.md

6. 本地測試
   $ npm run dev
   - 測試 / （繁中）
   - 測試 /en/ （英文）
   - 測試語言切換

7. 建置並部署
   $ npm run build
   $ git push
```

---

## 🎯 最佳實踐

### DO ✅

1. **使用 Astro i18n 靜態生成**
   - SEO 友善
   - 速度快
   - 成本低

2. **AI 輔助 + 人工校對**
   - OpenAI 做初譯（節省時間）
   - 人工確保品質（專業準確）

3. **繁中 → 英文 → 繁中驗證**
   - 避免大陸用詞
   - 確保台灣用語正確

4. **優先翻譯高價值頁面**
   - 首頁、產品頁、定價頁
   - 其他頁面可以漸進式翻譯

5. **建立翻譯記憶庫**
   - 紀錄專業術語翻譯
   - 保持一致性

### DON'T ❌

1. **不要即時翻譯**
   - 成本高
   - 速度慢
   - SEO 差

2. **不要完全依賴 AI**
   - AI 可能有技術術語錯誤
   - 需要人工檢查專業性

3. **不要翻譯所有內容**
   - 部落格文章可能只寫繁中
   - FAQ 可能只寫繁中
   - 根據受眾決定

4. **不要忘記 SEO**
   - 每個語言版本都要有正確的 hreflang
   - 每個語言都要有獨立的 sitemap

---

## 🌐 SEO 優化

### 3.1 設定 hreflang

在 `BaseLayout.astro` 的 `<head>` 中：

```astro
---
const { lang } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const alternateURL = lang === 'zh-TW' 
  ? new URL(`/en${Astro.url.pathname}`, Astro.site)
  : new URL(Astro.url.pathname.replace(/^\/en/, ''), Astro.site);
---

<head>
  <!-- Canonical -->
  <link rel="canonical" href={canonicalURL} />
  
  <!-- hreflang -->
  <link rel="alternate" hreflang="zh-TW" href={lang === 'zh-TW' ? canonicalURL : alternateURL} />
  <link rel="alternate" hreflang="en" href={lang === 'en' ? canonicalURL : alternateURL} />
  <link rel="alternate" hreflang="x-default" href={lang === 'zh-TW' ? canonicalURL : alternateURL} />
</head>
```

### 3.2 多語言 Sitemap

Astro sitemap 會自動支援多語言。

---

## 📝 實作時程

### Phase 1: 基礎設定（1 天）
- [x] 更新 astro.config.mjs
- [ ] 建立 i18n 資料夾結構
- [ ] 建立翻譯工具函式
- [ ] 建立語言切換元件

### Phase 2: 內容準備（2-3 天）
- [ ] 建立繁中 JSON 翻譯檔
- [ ] 建立英文 JSON 翻譯檔（AI 輔助）
- [ ] 人工校對英文翻譯
- [ ] 準備英文頁面內容

### Phase 3: 頁面開發（1 週）
- [ ] 更新所有頁面支援 i18n
- [ ] 建立英文版頁面
- [ ] 測試語言切換
- [ ] SEO 優化

### Phase 4: 測試與上線（2-3 天）
- [ ] 功能測試
- [ ] SEO 驗證
- [ ] 效能測試
- [ ] 正式上線

**總計：約 2 週**

---

## 🔧 維護指南

### 新增頁面時

1. 建立繁中版本
2. 執行 `npm run translate`
3. 校對英文版本
4. 建立英文頁面
5. 測試兩個語言版本

### 更新內容時

1. 更新繁中內容
2. 重新執行翻譯（僅翻譯變更部分）
3. 校對變更
4. 更新英文內容
5. 重新部署

---

## 📚 參考資源

### Astro 官方文檔
- [Astro i18n Routing](https://docs.astro.build/en/guides/internationalization/)
- [Content Collections](https://docs.astro.build/en/guides/content-collections/)

### OpenAI API
- [Chat Completions API](https://platform.openai.com/docs/guides/chat)
- [Best Practices for Translation](https://platform.openai.com/docs/guides/translation)

### i18n 最佳實踐
- [Google i18n Guidelines](https://developers.google.com/international)
- [W3C i18n Techniques](https://www.w3.org/International/techniques/)

---

## 🎯 總結與建議

### ✅ 推薦方案

**使用 Astro i18n 靜態生成 + OpenAI 輔助翻譯**

**理由：**
1. 生產環境零成本
2. SEO 完美支援
3. 速度極快
4. AI 提升效率
5. 人工確保品質

**實施步驟：**
1. 先完成繁中網站開發
2. 再加入 i18n 支援
3. 使用 OpenAI 輔助翻譯
4. 人工校對確保專業
5. 部署多語言版本

**預期效果：**
- 開發成本：低（2 週）
- 翻譯成本：極低（< $1）
- 執行成本：$0
- SEO 效果：優秀
- 使用者體驗：極佳

---

**建立日期**: 2026-01-25  
**維護者**: 智序資訊工作室  
**狀態**: 📋 待實作
